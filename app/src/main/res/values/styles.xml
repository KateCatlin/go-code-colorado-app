<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/teal</item>
        <item name="colorPrimaryDark">@color/teal</item>
        <item name="colorAccent">@color/teal</item>
    </style>

    <style name="chat_styles">
        <item name="backgroundColor">@color/background_white</item>
        <item name="timestampTextColor">@color/teal</item>
        <item name="localBubbleBackground">@color/teal</item>
        <item name="localBubbleTextColor">@color/text_white</item>
        <item name="externalBubbleBackground">@color/teal</item>
        <item name="externalBubbleTextColor">@color/text_white</item>
        <item name="snackbarBackground">@color/teal</item>
        <item name="snackbarTitleColor">@color/text_white</item>
        <item name="snackbarButtonColor">@color/text_white</item>
    </style>

</resources>
